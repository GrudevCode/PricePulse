# Copy to .env.local for local dev (never commit real secrets).
# Production: set the same VITE_* names in Vercel → Settings → Environment Variables, then redeploy.

VITE_BACKEND_ORIGIN=https://pricepulse-291t.onrender.com
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...
